# Менеджер задач с базой данных PostgreSQL

Простое консольное приложение для управления задачами с использованием PostgreSQL в качестве базы данных.

## Функциональность

- 📋 Просмотр списка задач
- ➕ Добавление новых задач
- ✏️ Обновление существующих задач
- ❌ Удаление задач
- 🗃️ Хранение данных в PostgreSQL

## Требования

- Python 3.x
- PostgreSQL
- Библиотека `psycopg2`

## Установка

1. Установите необходимые зависимости:
```bash
pip install psycopg2-binary
```

2. Настройте базу данных PostgreSQL:
   - Создайте базу данных с именем `task_manager_db`
   - Или измените параметры подключения в коде

3. Настройте параметры подключения к БД в файле:
```python
CONNECT_DB = {
    'user': 'postgres',      # Ваш пользователь PostgreSQL
    'password': 'admin',     # Ваш пароль
    'database': 'task_manager_db',  # Название базы данных
    'host': 'localhost',     # Хост БД
    'port': 5432,           # Порт PostgreSQL
}
```

## Использование

Запустите приложение:
```bash
python main.py
```

### Доступные команды:

- **1** — Просмотреть все задачи
- **2** — Добавить новую задачу
- **3** — Удалить задачу по ID
- **4** — Обновить задачу по ID
- **0** — Выйти из приложения

### Приоритеты задач:
- Низкий
- Средний  
- Высокий

## Структура базы данных

При первом запуске автоматически создается таблица `tasks` со следующей структурой:

```sql
CREATE TABLE IF NOT EXISTS tasks
(
    id       SERIAL PRIMARY KEY,
    title    TEXT NOT NULL,
    priority TEXT NOT NULL
)
```

## Особенности

- Автоматическая инициализация базы данных при первом запуске
- Устойчивость к ошибкам ввода
- Подтверждение операций (удаление, обновление)
- Чистый и понятный интерфейс
